export enum ECoursePublish {
  NotPublished = 'notPublished',
  Rejected = 'rejected',
  Pending = 'pending',
  Published = 'published',
}

export enum ECourseImageType {
  Banner = 'banner',
  CoverImage = 'coverImage',
}

export enum ECourseVideoType {
  VideoIntroduction = 'videoIntroduction',
}
